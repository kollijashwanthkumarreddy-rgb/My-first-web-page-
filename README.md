# Jashu PWA

Updated PWA with top-left 3-line menu, Edit, Export/Import JSON, Black/White/Blue/Green themes, playlists, favourites, pin/unpin, add-to-playlist, work-hours and workouts. The supplied photo is used for the PWA icon.

Run with `python -m http.server 8080` and open `http://localhost:8080`.
